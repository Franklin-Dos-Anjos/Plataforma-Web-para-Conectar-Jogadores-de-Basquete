// src/app/models/enums.ts

export enum Position {
  ARMADOR = 'ARMADOR',
  ALA = 'ALA',
  ALA_PIVO = 'ALA_PIVO',
  PIVO = 'PIVO',
}

export enum Level {
  INICIANTE = 'INICIANTE',
  INTERMEDIARIO = 'INTERMEDIARIO',
  AVANCADO = 'AVANCADO',
}

export enum Availability {
  MANHA = 'MANHA',
  TARDE = 'TARDE',
  NOITE = 'NOITE',
}
